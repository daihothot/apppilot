{
  "name": "apppilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "apppilot": "./src/startup/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run typecheck && bun run test && bun build src/startup/index.ts --target=bun --outfile=/tmp/apppilot-check.js",
    "start": "bun src/startup/index.ts",
    "build:plugin": "rm -rf dist && mkdir -p dist/.codex-plugin dist/skills dist/tools && bun build src/startup/index.ts --target=bun --compile --outfile=dist/apppilot && cp .codex-plugin/plugin.json dist/.codex-plugin/plugin.json && cp -R skills/apppilot dist/skills/apppilot && cp src/platforms/unityeditor/tools/AppPilotUnityBuild.cs dist/tools/AppPilotUnityBuild.cs"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
